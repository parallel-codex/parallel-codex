# build-ts composite action

This directory will hold the composite action that builds, tests, and packages the Parallel Codex TypeScript toolkit. It will be shared by workflows that need a consistent TS build pipeline.
