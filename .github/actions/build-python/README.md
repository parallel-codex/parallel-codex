# build-python composite action

This folder is reserved for the composite action that will lint, test, and package the Parallel Codex Python toolkit so workflows can reuse the same uv pipeline without duplication.
